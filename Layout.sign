SIGNATURE Layout

IMPORT WinConfig ONLY point
       String COMPLETELY
       Real COMPLETELY
       Seq[posWord] COMPLETELY

-- FUN layout : metrics ** seq[par] -> seq[posWord]

TYPE posWord == posWord(text : string, pos : point, width : real)

FUN testData : seq[posWord]


